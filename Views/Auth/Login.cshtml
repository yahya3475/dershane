@{
    ViewData["Title"] = "Login";
}

<div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="card shadow-lg border-0 rounded-4 p-4" style="max-width: 400px; width: 100%;">
        <div class="text-center mb-4">
            <h2 class="fw-bold text-primary">🔐 Log In</h2>
            <p class="text-secondary">Log in to access dershane</p>
        </div>

        @if (ViewBag.Hata != null)
        {
            <div class="alert alert-danger text-center" role="alert">
                @ViewBag.Hata
            </div>
        }

        <form method="post">
            <div class="mb-3">
                <label for="schoolnumber" class="form-label fw-semibold">School Number</label>
                <input name="schoolnumber" type="text" class="form-control" id="schoolnumber" placeholder="Enter your school number" required>
            </div>

            <div class="mb-4">
                <label for="password" class="form-label fw-semibold">Password</label>
                <input name="password" type="password" class="form-control" id="password" placeholder="••••••••" required>
            </div>

            <div class="d-grid">
                <button type="submit" class="btn btn-primary btn-lg rounded-pill">
                    🚀 Log in
                </button>
            </div>
        </form>
    </div>
</div>
